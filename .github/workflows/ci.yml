name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        default: 'dev'
        type: choice
        options: [dev, staging, prod]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/test-apps-repo

jobs:
  validate:
    name: Validate Kustomize
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install kustomize
        run: |
          TAG=$(curl -sL "https://api.github.com/repos/kubernetes-sigs/kustomize/releases/latest" | python3 -c "import json,sys; print(json.load(sys.stdin)['tag_name'])")
          curl -sSL "https://github.com/kubernetes-sigs/kustomize/releases/download/${TAG}/kustomize_${TAG#kustomize\/}_linux_amd64.tar.gz" -o kustomize.tar.gz
          tar xzf kustomize.tar.gz
          chmod +x kustomize
          ./kustomize version
      - name: Validate all overlays
        run: |
          kustomize build environments/dev
          kustomize build environments/staging
          kustomize build environments/prod

  build-push:
    name: Build & Push
    needs: validate
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
      id-token: write
    outputs:
      sha: ${{ steps.sha.outputs.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=,format=short
            type=ref,event=branch

      - name: Record short SHA
        id: sha
        run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha,scope=main
          cache-to: type=gha,mode=max,scope=main
          provenance: false
          sbom: false

  deploy:
    name: Deploy to k3s
    needs: build-push
    runs-on: ubuntu-24.04
    if: ${{ false }}  # Disabled: requires SSH_KEY secret (add via repo Settings → Secrets)
    environment: ${{ github.event.inputs.environment || 'dev' }}
    concurrency:
      group: deploy-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            environments
          sparse-checkout-cone-mode: false

      - name: Install kubectl
        run: |
          mkdir -p /usr/local/bin
          curl -sLo /usr/local/bin/kubectl "https://dl.k8s.io/release/$(curl -sL https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
          chmod +x /usr/local/bin/kubectl

      - name: Get kubeconfig
        run: |
          echo "$SSH_KEY" > /tmp/ssh_key
          chmod 600 /tmp/ssh_key
          mkdir -p ~/.kube
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 -i /tmp/ssh_key \
              root@2.26.98.178 \
              "cat /etc/rancher/k3s/k3s.yaml" | \
              sed 's|127.0.0.1|2.26.98.178|g' > ~/.kube/config
          chmod 600 ~/.kube/config
          rm /tmp/ssh_key
        env:
          SSH_KEY: ${{ secrets.SSH_KEY }}

      - name: Build and apply manifest
        env:
          IMAGE_TAG: ${{ needs.build-push.outputs.sha }}
          ENV: ${{ github.event.inputs.environment || 'dev' }}
        run: |
          cd environments/$ENV
          kustomize build . --load-restrictor=LoadRestrictionsNone | \
            sed 's|nginx:alpine|ghcr.io/${{ github.repository_owner }}/test-apps-repo:'"$IMAGE_TAG"'|g' | \
            kubectl apply --server-side -f -
          echo "Deployed: ghcr.io/${{ github.repository_owner }}/test-apps-repo:$IMAGE_TAG"

      - name: Verify deployment
        run: |
          kubectl rollout status deployment/test-app-${ENV} --timeout=60s || true
          kubectl get pods -l app=test-app